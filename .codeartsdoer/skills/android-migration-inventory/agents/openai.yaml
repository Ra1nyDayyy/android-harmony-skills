interface:
  display_name: "Android 迁移语义盘点"
  short_description: "自动定位页面与功能，并用逐页机器门禁防止误判通过"
  default_prompt: "使用 $android-migration-inventory 自动分析当前 Android 项目的页面、UI、功能、状态和跳转关系，并以逐页原子证据计算验收结论。"
policy:
  allow_implicit_invocation: true
