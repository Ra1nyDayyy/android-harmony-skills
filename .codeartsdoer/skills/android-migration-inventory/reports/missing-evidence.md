# Missing evidence

Status: `missing evidence`

- The upstream YAO umbrella CLI is Unix-bound through `fcntl` and was not run on this Windows host; individual cross-platform checks are used.
- Output cases are file-backed fixtures, not provider-executed model runs; token and latency telemetry are unavailable.
- Blind A/B reviewer decisions are pending.
- CodeArts task identities are not authenticated against a remote service API.
- Reflection targets, server-driven UI, WebView content, special accounts, and device-only states remain migration-run evidence obligations; governance reports cannot mark them covered.
