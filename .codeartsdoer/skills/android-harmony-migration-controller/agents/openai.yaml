interface:
  display_name: "安卓到鸿蒙迁移总控"
  short_description: "治理四阶段迁移、机器门禁、人工审核与返工"
  default_prompt: "使用 $android-harmony-migration-controller 在同一个任务中执行 Phase 1-4。每阶段先计算机器 Gate，再生成简明审核包并停在 WAITING_HUMAN_REVIEW；只有当前 Gate 的人工批准才能进入下一阶段。"
policy:
  allow_implicit_invocation: true
