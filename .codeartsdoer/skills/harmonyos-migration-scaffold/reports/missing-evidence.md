# Missing evidence

Status: `missing evidence`

- The YAO umbrella CLI cannot run on this Windows host because its evidence store imports Unix-only `fcntl`; individual cross-platform checks are used.
- Output cases are recorded file-backed fixtures, not provider-backed model executions; reviewer adjudication is pending.
- CodeArts task identities are not remotely authenticated.
- A real DevEco SDK, emulator, HAP signature, install, launch, route opening, and emulator screenshot must be evidenced inside each migration run; package governance cannot infer them.
