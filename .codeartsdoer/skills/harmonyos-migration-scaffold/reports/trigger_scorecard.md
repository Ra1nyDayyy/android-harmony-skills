# Trigger scorecard

- Public cases: 9 (3 should-trigger, 3 should-not-trigger, 3 near-neighbor).
- Precision: `1.000`; recall: `1.000`; false positives: `0`; false negatives: `0`.
- Semantic configuration: `evals/semantic_config.json`.
- Limitation: this is a deterministic public fixture suite. Blind holdout and observed CodeArts routing telemetry are `missing evidence`.
